//  Por favor levantar el proyecto con nodemon
const lista = [...Array(20)];

console.log("Un mensaje");

console.log("Agregué esto y se reinició");

lista.forEach((_element, index) => {
  //   Descomenta la siguiente linea, guarde los cambios y luego revise su terminal
  //   console.log(index);
});
